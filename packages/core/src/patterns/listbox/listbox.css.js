

// TODO: ARIA highly recommends using one of select, radiogroup (for single selection), or checkbox (for multiple selection) rather than this role.
export default /*css*/`
.listbox, :where([role="listbox"]) {
    
}`;